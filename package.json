{
  "name": "jspsych-wrap",
  "version": "1.0.0",
  "description": "Express server and client utilities for jsPsych experiments — npm replacement for the PHP/submodule wrapper",
  "private": true,
  "main": "./dist/server/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
